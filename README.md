# Chatterbox — destroy after failed upload, but try sending logs first

This version adds:
- upload retries
- destroy instance after failed upload retries
- final log-only upload attempt before destroy
